# Django settings
SECRET_KEY=dev-change-me
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# JWT lifetimes
JWT_ACCESS_MINUTES=60
JWT_REFRESH_DAYS=7

# CORS (set your frontend dev URL)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# PostgreSQL (leave blank to use SQLite fallback)
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
